{
  ".cpp": {
    "compileCommand": [
      "clang++",
      "${file}",
      "-o",
      "${fileDirname}/${fileBasenameNoExtension}"
    ],
    "runCommand": [
      "${fileDirname}/${fileBasenameNoExtension}"
    ]
  }
}